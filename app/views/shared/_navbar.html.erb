<nav class="navbar navbar-default navbar-static-top">
	<div class="container">
		<div class="navbar-header">
			
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to "Airbnb", root_path, class: "navbar-brand" %>
		</div>
		<div id="flash">  
      <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
      <% end %>
    </div>
    
    
		<div id="navbar" class="navbar-collapse collapse">
			<ul class="nav navbar-nav navbar-right">

				<% if(!signed_in?)%>
					<li><%= link_to "Sign In", sign_in_path %></li>
					<li><%= link_to "Sign Up", sign_up_path %></li>
				<% else %>
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="buttpn" aria-haspopup="true" aria-expanded="false">
						<% if current_user.avatar.url != nil %>
							<%= image_tag current_user.avatar.url, class: "img-circle avatar-small" %>&nbsp;
						<% end %>
						<%= current_user.first_name %> <%=current_user.last_name%><span class="caret"></span>
						</a>
						<ul class="dropdown-menu">
							<li><%= link_to "Your Bookings", show_all_booking_path(current_user)%> </li>	
							<li><a href="#">Wish Lists</a></li>	
							<li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
							<li role="seperator" class="divider"></li>
							<li><%= link_to "My Profile", user_path(current_user) %></li>
							<li><%= link_to "Log Out", sign_out_path, method: :delete%></li>
						</ul>
					</li>
				
				<li>
					<a href="#"><i class="fa fa-envelope-o"></i> Messages</a>
				</li>
				<li>
					<%= link_to 'Become a host', new_listing_path %>
				</li>	
				<% end %>
			</ul>
		</div>
	</div>
</nav>